// Jest configuration for backend tests
// Jest হলো একটা টেস্টিং টুল (সফটওয়্যার পরীক্ষা করার যন্ত্র)

export default {
  // ============= টেস্ট পরিবেশ =============
  // testEnvironment: কোথায় টেস্ট চলবে?
  // 'node' মানে নোড.জেএস পরিবেশে টেস্ট হবে (ব্রাউজার না)
  testEnvironment: 'node',
  
  // ============= ট্রান্সফর্ম সেটিংস =============
  // transform: কীভাবে কোড পরিবর্তন করবে?
  // {} মানে কোনো বিশেষ ট্রান্সফর্মার ব্যবহার করবে না
  transform: {},
  
  // ============= ইএস মডিউল সেটিংস =============
  // extensionsToTreatAsEsm: কোন ফাইলগুলোকে ইএস মডিউল হিসেবে দেখাবে?
  // ['.js'] মানে .js ফাইলগুলোকে ইএস মডিউল ধরা হবে
  extensionsToTreatAsEsm: ['.js'],
  
  // ============= মডিউল নাম ম্যাপিং =============
  // moduleNameMapper: ইমপোর্ট পাথ কিভাবে পরিবর্তন করবে?
  moduleNameMapper: {
    // '^(\\.{1,2}/.*)\\.js$': '$1' 
    // মানে: ./file.js থেকে .js বাদ দিয়ে ./file করবে
    '^(\\.{1,2}/.*)\\.js$': '$1',
  },
  
  // ============= সেটআপ ফাইল =============
  // setupFilesAfterEnv: টেস্ট শুরুর আগে কোন ফাইল চালাবে?
  // ['./setup.js'] মানে setup.js ফাইল আগে চালাবে (ডাটাবেজ কানেক্ট ইত্যাদি)
  setupFilesAfterEnv: ['./setup.js'],
  
  // ============= টেস্ট ফাইল চেনার প্যাটার্ন =============
  // testMatch: কোন ফাইলগুলো টেস্ট ফাইল?
  // '**/*.test.js' মানে সব জায়গায় .test.js দিয়ে শেষ যেকোনো ফাইল
  testMatch: ['**/*.test.js'],
  
  // ============= কাভারেজ সংগ্রহ (কতটুকু টেস্ট করেছি) =============
  // collectCoverageFrom: কোন ফোল্ডারের কোড টেস্ট কাভারেজ হিসাব করবে?
  collectCoverageFrom: [
    '../controllers/**/*.js',   // কন্ট্রোলার ফোল্ডারের সব .js ফাইল
    '../middlewares/**/*.js',   // মিডলওয়্যার ফোল্ডারের সব .js ফাইল
    '../models/**/*.js',        // মডেল ফোল্ডারের সব .js ফাইল
    '!**/node_modules/**',      // node_modules ফোল্ডার বাদ দাও
  ],
  
  // ============= কাভারেজ থ্রেশহোল্ড (কতটুকু টেস্ট করতে হবে) =============
  // coverageThreshold: কত শতাংশ টেস্ট করতে হবে?
  coverageThreshold: {
    global: {
      branches: 50,     // ব্রাঞ্চ (if-else) এর ৫০% টেস্ট করতে হবে
      functions: 50,    // ফাংশনের ৫০% টেস্ট করতে হবে
      lines: 50,        // লাইনের ৫০% টেস্ট করতে হবে
      statements: 50,   // স্টেটমেন্টের ৫০% টেস্ট করতে হবে
    },
  },
};